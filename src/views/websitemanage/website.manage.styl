@import "../../resource/mixins.styl"

.website-manage
    common-panel(0,20px)
    // flex-wrap wrap
    >.form-part
        width 100%
        display flex
        flex 0 1 auto
        flex-direction column
        .el-input__inner
            width 200px
        .el-date-editor
            width auto !important
        // margin-right 20px
        // flex-wrap wrap
        >.btn-group
            display flex
            flex 0 0 auto
            justify-content space-between
            align-items center
            >div
                display flex
                cursor pointer
                .btn-test
                    font-size 12px
                    height 36px
                    line-height 36px
                    margin-left 10px
        >.el-form--inline
            display flex
            flex 0 1 auto
            flex-wrap wrap
            >.el-form-item
                &:last-child
                    padding-left 20px
